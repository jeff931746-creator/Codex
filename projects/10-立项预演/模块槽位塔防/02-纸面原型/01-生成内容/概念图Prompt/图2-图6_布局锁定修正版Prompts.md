# 模块槽位塔防：图2-图6 布局锁定修正版 Prompt

## 使用规则

- 从图 2 开始全部使用这一版。
- 每一张都默认：
  - 图 1 是唯一母版布局
  - 不允许改变镜头、基地位置、右侧模块面板位置、车道宽度、上下分区
  - 只允许改变当前这一张的玩法状态

## 图2：掉落模块 修正版

```text
Use image 1 as the direct master reference.
Do not reinterpret or redesign the scene.

Create a corrected version of image 2 in the same gameplay concept series.

This image must keep the exact same battlefield layout as image 1:
- same camera angle
- same vertical composition
- same lane width and lane centerline
- same base position at the bottom
- same main cannon, city wall, reactor core, side defense nodes, and summoning beacon placement
- same right-side module panel position and same 4 empty embedded hardware slots
- same UI hierarchy and same overall art style

Only one major state change is allowed:
an elite enemy has just been killed, and a large glowing module loot drops onto the battlefield.

Very important composition rules:
- the dropped module must be the main visual focus
- place the dropped module near the center of the lane
- make it large, glowing, high-value, and obviously collectible
- add a strong loot beam, holographic highlight, or glow ring
- show the elite enemy defeat source near the drop
- the dropped module must look like a physical machine component or energy cartridge
- the defense line is still fighting, but the loot drop is the single key event

Critical correction:
do not change the scene layout from image 1
do not move the module panel
do not move the base
do not zoom in or zoom out
do not change the camera angle
do not redesign the interface

Negative guidance:
no backpack inventory
no card drop
no random gem loot
no tiny item
no vague reward
no skill button menu
```

## 图3：安装第一个模块 修正版

```text
Use image 1 as the master layout reference and image 2 as the immediate state reference.
Do not redesign the scene.

Create image 3 of the same gameplay concept series.

Lock the exact same layout as image 1:
- same camera angle
- same vertical composition
- same lane width and centerline
- same base position
- same right-side module panel position
- same overall UI hierarchy

This image must focus on one clear event:
the first module has just been installed into one of the fixed hardware slots.

Important visual goals:
- exactly 1 of the 4 module slots is occupied
- that installed module must look like a cartridge locked into a permanent machine socket
- glowing energy cables now pulse from that installed module into the reactor core and defense line
- the battlefield reaction should be immediately visible
- show one dominant combat change such as stronger cannon fire or a clear burning explosion effect
- the other 3 module slots remain empty or inactive

Critical correction:
do not change the layout inherited from image 1
do not move the base or module panel
do not zoom the camera
do not turn the panel into a skill UI
```

## 图4：第二模块触发联动 修正版

```text
Use image 1 as the master layout reference and image 3 as the state reference.
Do not redesign the scene.

Create image 4 of the same gameplay concept series.

Lock the exact same layout as image 1:
- same camera angle
- same vertical composition
- same lane width
- same base position
- same right-side module panel position
- same UI hierarchy

This image must focus on one clear event:
a second module is now installed, and the interaction between modules visibly changes the battle.

Installed modules:
- Fire Core
- Chain Coil

Visual goals:
- exactly 2 of the 4 hardware slots are occupied
- both installed modules must feel physically connected into the same defense system
- energy cables should glow more strongly than before
- the battlefield should clearly show chain-burning or lightning-linked explosions across multiple enemies
- communicate synergy, not two isolated effects
- the defense line now feels more upgraded and more engineered than image 3

Critical correction:
do not change the layout inherited from image 1
do not shift any UI panel
do not move the battle lane
do not alter the framing
```

## 图5：Build 成型 修正版

```text
Use image 1 as the master layout reference and image 4 as the state reference.
Do not redesign the scene.

Create image 5 of the same gameplay concept series.

Lock the exact same layout as image 1:
- same camera angle
- same vertical composition
- same lane width and lane centerline
- same base position
- same module panel position
- same UI hierarchy

Installed modules:
- Fire Core
- Chain Coil
- Ice Crystal
- Barracks Beacon

The battlefield must clearly show that these modules are modifying the entire defense line:
- all 4 hardware slots are now visibly installed and active
- burning explosions across enemy groups
- chain lightning jumping between monsters
- frozen and slowed enemies in ice zones
- summoned soldiers fighting in front of the wall
- support turrets and defense nodes visibly reacting to the installed modules
- the whole defense line feels powered and transformed by the module setup

Critical correction:
preserve the exact same layout from image 1
do not make it feel like 4 active skills
make it feel like one reconfigured defense engine
```

## 图6：Boss 波检验 修正版

```text
Use image 1 as the master layout reference and image 5 as the state reference.
Do not redesign the scene.

Create image 6 of the same gameplay concept series.

Lock the exact same layout as image 1:
- same camera angle
- same vertical composition
- same lane width
- same base position
- same module panel position
- same UI framework

This image must focus on the final validation moment:
the completed module build is being tested by a boss wave.

Boss battle requirements:
- a large boss enemy pushes from the top toward the base
- the battlefield is under intense pressure
- all 4 module slots are visibly active and connected into the defense core
- show burning, chain lightning, freezing control, and summoned frontline units all working together
- the fight should feel dangerous, but the build is holding

Critical correction:
do not turn this into a separate boss arena
do not change the layout inherited from image 1
do not move or hide the module panel
do not reframe the battle
```
