# Overrides

Use this directory for project-specific deviations from shared defaults.

Suggested structure:

```text
overrides/
  my-project/
    rules/
    skills/
    references/
```

Prefer overrides to forking shared assets when only one project needs the exception.
